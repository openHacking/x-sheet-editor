import { Widget } from '../../../lib/Widget';
import { cssPrefix } from '../../../config';

class SplitTop extends Widget {
  constructor() {
    super(`${cssPrefix}-table-split-top`);
  }
}

export { SplitTop };
