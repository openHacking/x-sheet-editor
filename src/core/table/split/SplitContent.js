import { Widget } from '../../../lib/Widget';
import { cssPrefix } from '../../../config';

class SplitContent extends Widget {
  constructor() {
    super(`${cssPrefix}-table-split-content`);
  }
}

export { SplitContent };
