import { Widget } from '../../../lib/Widget';
import { cssPrefix } from '../../../config';

class SplitLeftTop extends Widget {
  constructor() {
    super(`${cssPrefix}-table-split-left-top`);
  }
}

export { SplitLeftTop };
