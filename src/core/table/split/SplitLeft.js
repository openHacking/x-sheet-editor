import { Widget } from '../../../lib/Widget';
import { cssPrefix } from '../../../config';

class SplitLeft extends Widget {
  constructor() {
    super(`${cssPrefix}-table-split-left`);
  }
}

export { SplitLeft };
