import { Widget } from '../../../../lib/Widget';
import { cssPrefix } from '../../../../const/Constant';

class XFilterButton extends Widget {

  constructor() {
    super(`${cssPrefix}-x-filter-button`);
  }

}

export {
  XFilterButton,
};
